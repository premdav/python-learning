# variables
my_tuple = 'python', 'javascript', 'c', 'c++'\

# tuples are either written like above or ('thing', 'thing', 'thing')

print(type(my_tuple))
tuple_length = len(my_tuple)
print(tuple_length)

# indention is used for blocks of code instead of {}
for item in my_tuple:
  print(item)